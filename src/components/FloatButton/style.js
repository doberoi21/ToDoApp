import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  iconView: {
    height: 60,
    width: 60,
    borderRadius: 30,
    backgroundColor: 'rgb(0,120,247)',
    justifyContent: 'center',
    alignItems: 'center',
    position:'absolute',
    bottom:30,
    right:25
  },
});
