import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  headerContainer: {
    height: 60,
  },
  headerText: {
    fontSize: 30,
    marginLeft: 20,
    fontWeight: 'bold',
    fontFamily: 'Poppins-Bold',
  },
});
